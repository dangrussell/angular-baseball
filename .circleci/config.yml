version: 2.1
jobs:
  build:
    #working_directory: ~/angular-baseball
    docker:
        - image: 'circleci/node:latest'
    steps:
        - checkout
        - run:
            name: 'List the dir'
            command: |
                ls -al
                cd angular-baseball
                ls -al
